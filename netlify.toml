[build]
  command = "cd vite-project && npm ci && npm run build"
  publish = "vite-project/dist"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command = "cd vite-project && npm run dev"
  port = 5173
